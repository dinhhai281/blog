import React, { FC } from 'react';
import Layout from '../components/layout';

const About: FC = () => {
  return (
    <Layout>
      <div>About page</div>
    </Layout>
  );
};

export default About;
