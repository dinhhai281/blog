import Layout from '@components/layout';
import React, { FC } from 'react';

const About: FC = () => {
  return (
    <Layout>
      <div>About page</div>
    </Layout>
  );
};

export default About;
